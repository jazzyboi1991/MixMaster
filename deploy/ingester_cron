# MixMaster Wikipedia Ingester - Daily Schedule
# 매일 새벽 1시에 Wikipedia 문서 수집 및 Vector DB 구축
0 1 * * * cd /home/opc/MixMaster && /usr/bin/python3.11 src/ingester.py >> /mnt/mixmaster-data/logs/ingester.log 2>&1

# crontab에 추가하는 방법:
# crontab -e
# 위 라인을 복사해서 붙여넣기
